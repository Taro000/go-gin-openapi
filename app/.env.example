########################
# Server
########################
# Ginサーバの待受ポート
PORT=8080

########################
# Database (MySQL)
########################
# どちらかを設定:
# - DB_DSN を直接指定（優先）
# - もしくは DB_HOST/DB_PORT/DB_USER/DB_PASSWORD/DB_NAME を指定
#
# 例: root:root@tcp(db:3306)/go-gin-webapi?parseTime=true&charset=utf8mb4&collation=utf8mb4_ja_0900_as_cs&loc=Asia%2FTokyo
# DB_DSN=

# devcontainer の mysql サービス名は `db`
DB_HOST=db
DB_PORT=3306
DB_USER=root
DB_PASSWORD=root
DB_NAME=go-gin-webapi

########################
# Auth (Firebase)
########################
# register/login（メール/パスワード）に必須
FIREBASE_API_KEY=

# bearer認証（IDトークン検証）や logout の revoke を使う場合に推奨
# - 本番: サービスアカウントを指定
# - ローカル: エミュレータ利用も可
FIREBASE_PROJECT_ID=

# どちらかを指定:
# - JSONファイルのパス（コンテナ内のパスにすること）
FIREBASE_CREDENTIALS_FILE=
# - JSON文字列を直接渡す（改行は \\n にするか、1行JSONにする）
FIREBASE_SERVICE_ACCOUNT_JSON=

# Firebase Auth Emulator を使う場合
# 例: localhost:9099
FIREBASE_AUTH_EMULATOR_HOST=

# 開発用: bearer認証をバイパスしたい場合 true
# その場合、各リクエストに `X-User-Id: <uid>` を付ければ認可が通ります。
AUTH_BYPASS=false


